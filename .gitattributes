# Only use LFS for very large design files that aren't needed for GitHub Pages
*.psd filter=lfs diff=lfs merge=lfs -text
*.ai filter=lfs diff=lfs merge=lfs -text
*.sketch filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text
*.rar filter=lfs diff=lfs merge=lfs -text

# Don't use LFS for files needed in GitHub Pages deployment
*.png text
*.jpg text
*.jpeg text
*.gif text
*.svg text
*.html text
*.css text
*.js text